class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        maxWords = 0
        for i in sentences:
            maxWords = max(maxWords, i.count(" ")+1)
        return maxWords
